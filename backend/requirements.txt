# ============================================================
# CPEDS-X Backend Requirements
# Python 3.10+
# ============================================================
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
imbalanced-learn==0.12.3
lightgbm==4.3.0
xgboost==2.0.3
shap==0.45.1
boto3==1.34.130
requests==2.32.3

# ---- Authentication ----
# The auth layer runs with zero extra installs (stdlib PBKDF2 + HS256 fallbacks),
# but transparently upgrades to these industry-standard libraries when present.
PyJWT==2.8.0
bcrypt==4.1.3

# ---- Hosted storage (Supabase / Postgres) ----
# Required only for cloud deployment (Render + Vercel), where the local filesystem
# is ephemeral. When SUPABASE_URL + SUPABASE_KEY are set the auth layer uses
# Supabase; otherwise it falls back to a local SQLite file for development.
supabase>=2.7.0,<3.0.0
