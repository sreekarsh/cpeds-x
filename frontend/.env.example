# Local dev - point frontend at local FastAPI backend.
# For Vercel production, set VITE_API_BASE_URL to your Render URL in the
# Vercel dashboard (Project Settings -> Environment Variables).
VITE_API_BASE_URL=http://localhost:8000
